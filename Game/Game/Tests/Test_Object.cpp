/*#include <stdio.h>
#include <stdlib.h>

#include "Object.h"
#include "Engine.h"

int main()
{
	Engine engine;
	Vector coord(1, 2);
	Vector veloc(3, 4);
	//sf::Sprite sprite;

	Object test(coord, veloc, 10, 2);
	Object test2(coord, veloc, 12, 4);
	test.Dump();
	test2.Dump();
	engine.addObject(&test);
	engine.addObject(&test2);
	engine.Dump();
	system("pause");
	return 0;
}*/